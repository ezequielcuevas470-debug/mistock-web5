import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Otro Flow Barber Shop',
  description: 'Atención VIP exclusiva y cortes de autor. Reserva tu turno.',
  openGraph: {
    title: 'Barbería VIP | Cortes de Autor & Experiencia Premium',
    description: 'Atención VIP exclusiva y cortes de autor.',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
  twitter: {
    card: 'summary_large_image',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body className={inter.className}>{children}</body>
    </html>
  );
}